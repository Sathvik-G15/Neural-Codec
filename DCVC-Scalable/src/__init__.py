"""Legacy Path B \u2014 intentionally inert.

The active training / evaluation code lives in
    DCVC-repo/DCVC-family/DCVC/
which integrates directly with Microsoft's DCVC bitstream.

This package is preserved only so historical imports do not break.
"""
__version__ = "0.0.0-legacy"
